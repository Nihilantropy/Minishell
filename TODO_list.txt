1) Make the here_doc redirection works perfectly.
2) See the cat | cat pipeline.
3) Check eventual memory leaks.
4) Check if we want our shell to work exactly like the bash or not. The bash shell
	can execute the pipeline not perfectly in order. The parent process doesn't necessary
	wait the exit of the child process. Check this thing if it's an evaluation issue.
5) Check the perror on the unset PATH event