Implement NON-INTERACTIVE MODE. ex: "./minishell -c echo hello; ls". This calls the programm with the arguments to parse
and to execute.

We must redo the signals because they don't work in the best way

See the cat | cat pipeline. (in general see the pipe)

Check eventual memory leaks.

Check the perror on the unset PATH event

Check the execve path. The programs can be executed from any directory, not only the PATH